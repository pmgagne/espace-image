description="Synthesizes the interview findings into a spec.md file."
prompt = """
Now that the interview is finished, your goal is to synthesize all our exchanges into a `SPECS.md` file.

Use the `write_file` tool to create `spec.md` in the root of the workspace (or appropriate location). The file must include:
- The objectives
- The technical architecture
- The data models
- The testing strategy

Wait for my review before proceeding to the next steps.
"""
